export const enum SongFee {
  free = 0,
  vip,
}

export const enum PlayMode {
  linear = 0,  // 顺序播放
  random,      // 随机播放
  circle       // 单曲循环
}

export const enum OriginSong {
  unknown = 0,  // 未知
  origin,   // 原创
  cover,   // 翻唱
}